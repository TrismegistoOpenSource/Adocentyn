-- userprefs.lua - tastiera e puntatore.
hl.config({
    input = {
        kb_layout    = "it",
        follow_mouse = 1,
        sensitivity  = 0,

        touchpad = {
            natural_scroll       = true,
            tap_to_click         = true,
            disable_while_typing = true,
        },
    },
})
